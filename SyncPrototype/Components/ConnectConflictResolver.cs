namespace SyncPrototype.Components
{
    /// <summary>
    /// Determines how to resolve a conflict between a change found between a connect instance and a client instance, where the connect is always chosen given that it has changed.
    /// </summary>
    public class ConnectConflictResolver
    {
    }
}
